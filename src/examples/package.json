{
  "scripts": {
    "clean": "mkdirp ./dist && rimraf ./dist/*",
    "copy": "ncp ./src/assets ./dist/assets",
    "start": "npm run clean && npm run copy && parcel ./src/index.html",
    "build": "npm run clean && npm run copy && parcel build ./src/index.html"
  },
  "license": "MIT",
  "dependencies": {
    "gsap": "^3.12.5",
    "pixi.js": "^8.0.5"
  },
  "devDependencies": {
    "mkdirp": "^3.0.1",
    "ncp": "^2.0.0",
    "parcel": "^2.12.0",
    "rimraf": "^5.0.5"
  },
  "alias": {
    "pixi.js": "./node_modules/pixi.js"
  }
}
